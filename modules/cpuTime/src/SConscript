Import("env")

env.Append(CCFLAGS = ["-W", "-Wall", "-D_GNU_SOURCE"])
env.Append(LIBS = ["rt", "dl"])

env.SharedLibrary(target="collector", source=Glob("collector/*.c"))
env.SharedLibrary(target="marshal", source=Glob("marshal/*.cpp"))
